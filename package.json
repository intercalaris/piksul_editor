{
  "name": "Piksul",
  "version": "1.0.0",
  "description": "Pixel art corrector",
  "main": "server.js",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node --disable-warning=ExperimentalWarning --watch server.js",
    "migrate": "node --disable-warning=ExperimentalWarning data/migrate_db.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^8.2.0",
    "ejs": "^3.1.6",
    "express": "^4.17.1",
    "morgan": "^1.10.0",
    "multer": "^1.4.5-lts.1"
  }
}
